BINDIR = ../bin/
OCAMLINCLUDES += ../../OParse/src/

FILES[] =
  util
  ast
  inst
  parse
  gen
  vm
  camlsh

PROGRAM = camlsh
# OCAMLFLAGS += -i
OCAML_LIBS += ../../Oparse/src/oparse
OCAML_OTHER_LIBS += unix

.DEFAULT: $(OCamlProgram $(BINDIR)$(PROGRAM), $(FILES))

.PHONY: clean

clean:
  rm -f *.cm* *.o camlsh camlsh.opt