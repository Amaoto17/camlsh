BINDIR = ../bin/
OCAMLINCLUDES += ../../OParse/src/ ~/.opam/4.06.1/lib/re

FILES[] =
  util
  ast
  inst
  parse
  gen
  ctx
  builtin
  vm
  camlsh

PROGRAM = camlsh
# OCAMLFLAGS += -i
OCAML_OTHER_LIBS += unix oparse re

.DEFAULT: $(OCamlProgram $(BINDIR)$(PROGRAM), $(FILES))

.PHONY: clean

clean:
  rm -f *.cm* *.o $(BINDIR)camlsh $(BINDIR)camlsh.opt